app {
  client {
    address = "http://localhost:8000"
  }
  system {
    name = app-system
  }
}

akka {
  http {
    server {
      idle-timeout = 60 seconds
    }

    client {
      websocket {
        periodic-keep-alive-mode = pong
      }
    }
  }
}
